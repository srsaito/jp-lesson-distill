work/
.venv/
__pycache__/
*.pyc
.env

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Belt and braces: bd 1.0.2 dropped a stray duplicate of .beads/issues.jsonl here on
# every commit from a secondary checkout (jld-78z, fixed by bd 1.2.2). Kept so a
# downgrade cannot quietly reintroduce it.
/issues.jsonl
